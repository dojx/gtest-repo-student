#include "StringCalculator.h"
#include <cctype>
#include <algorithm>

int add(string input)
{
  if (input.empty()) {
    return 0;
  }
  if (any_of(begin(b), end(b), isalpha))
  {
    return 0;
  } 
  throw "invalid argument";
}
